{% set name = "tilupy" %}
{% set version = "2.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/tilupy-{{ version }}.tar.gz
  sha256: 8b8caf12cb20ca28ddfb3b20705232ed925c4c5da53bd4f90fcef61a9109b01d

build:
  entry_points:
    - tilupy_plot = tilupy.cmd:_tilupy_plot
    - tilupy_to_raster = tilupy.cmd:_tilupy_to_raster
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - seaborn
    - requests
    - scipy
    - pytopomap
    - rasterio
    - shapely

test:
  imports:
    - tilupy
  commands:
    - pip check
    - tilupy_plot --help
    - tilupy_to_raster --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/marcperuz/tilupy
  summary: Thin-layer models unified processing tool
  license: CECILL-C
  license_file: LICENSE

extra:
  recipe-maintainers:
    - marcperuz
